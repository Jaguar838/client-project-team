import Loader from 'react-loader-spinner';
import './Spinner.module.scss';

const Spinner = () => {
  return <Loader type="Rings" color="#00BFFF" height={42} width={42} />;
};

export default Spinner;
