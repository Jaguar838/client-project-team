import Loader from 'react-loader-spinner';
import './Spinner.module.scss';

const Spinner = () => {
  return <Loader type="Rings" color="#4A56E2" height={42} width={42} />;
};

export default Spinner;
